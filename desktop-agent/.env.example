# URL pointing to your active Express API Server Ingestion endpoints
# Default emulator localhost is http://localhost:3000
WELLNESS_SERVER_URL=http://localhost:3000

# User JWT authorization token received from signup or login response payload
WELLNESS_AUTH_TOKEN=your_jwt_token_received_at_signup_or_login
